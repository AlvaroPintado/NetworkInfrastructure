{
  "name": "sensor-simulator",
  "version": "1.0.0",
  "description": "Simulador de sensor que envía datos vía GET y POST",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
