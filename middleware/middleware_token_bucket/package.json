{
  "name": "middleware_token_bucket",
  "version": "1.0.0",
  "description": "Middleware implementing the token bucket algorithm.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0",
    "rate-limiter-flexible": "^7.1.1"
  }
}
